name: OpenSSF Scorecard

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/scorecard.yml'
      - '.github/workflows/ci.yml'
      - '.github/workflows/reusable-python.yml'
  schedule:
    - cron: '23 4 * * 3'
  workflow_dispatch:

permissions: {}

jobs:
  scorecard:
    permissions:
      contents: read
      actions: read
      security-events: write
      id-token: write
    uses: karnalooch/engineering-platform/.github/workflows/reusable-scorecard.yml@b34fda2ef31bf62e00422f8531202e2cccc3bc73
